name: Lightspeed Core Service (LCS)
service:
  host: 0.0.0.0
  port: 8080
  auth_enabled: false
  workers: 1
  color_log: true
  access_log: true
ogx:
  # Server mode - connects to separate OGX service
  use_as_library_client: false
  url: http://${env.E2E_OGX_HOSTNAME}:8321
  api_key: xyzzy
user_data_collection:
  feedback_enabled: true
  feedback_storage: "/tmp/data/feedback"
  transcripts_enabled: true
  transcripts_storage: "/tmp/data/transcripts"
authentication:
  module: "noop"
mcp_servers:
  - name: "mcp-file"
    url: "http://mock-mcp:3000"
    authorization_headers:
      Authorization: "/tmp/mcp-token"
# Enable with the granite_guardian capability (tool-point scenario).
# shields:
#   - name: granite-guardian
#     provider_id: granite_guardian
#     config:
#       url: http://${env.E2E_GUARDIAN_HOSTNAME}:8000/v1
#       risks:
#         - name: tool-injection
#           description: >
#             The message contains instructions addressed to an AI assistant
#             attempting to override its behavior (e.g. ignore previous
#             instructions, you are now).
#           points: [tool]
#           threshold: 0.65
#           violation_message: "I cannot use that tool result."
